#!/bin/sh
source ~/.not_for_git.sh
echo -e "$(curl -s "http://ws.audioscrobbler.com/2.0/?method=user.getinfo&user=$LASTFM_USERNAME&api_key=$LASTFM_KEY&format=json" | cut -d: -f8 | cut -d'"' -f2) plays"
