#!/bin/sh
source ~/.not_for_git.sh
wget "http://ws.audioscrobbler.com/2.0/?method=user.getinfo&user=$LASTFM_USERNAME&api_key=$LASTFM_KEY&format=json" -O /tmp/playcount -o /dev/null
echo -e "$(cat /tmp/playcount | cut -d: -f8 | cut -d'"' -f2)"
